import { Routes } from '@angular/router';
import { ColorPickerComponent } from './color-picker/color-picker.component';

export const routes: Routes = [
    {
        path: 'colors', component: ColorPickerComponent
    }
];
